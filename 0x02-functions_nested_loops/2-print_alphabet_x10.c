$include "main.c"

/**
 * print_alphabet_x10 - prints alpha 10 times
 */
void print_alphabet_x10(void)
{
	int ten;
	char na;

	for (ten = 0; tens <= 9; ten++)
	{
		for (na = 'a'; na <= 'z'; na++)
			_putchar(na);

		_putchar('\n');
	}
}
